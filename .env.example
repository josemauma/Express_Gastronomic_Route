# .env.example
# Rename to `.env`, drop it in the project root, and swap the placeholders
# with your real secrets / paths.

# ── API KEYS ────────────────────────────────────────────────
API_GOOGLE_PLACES=your_google_places_api_key   # Mandatory – powers place look‑ups
API_WEATHER_KEY=your_weather_api_key           # Mandatory – drives weather data

# ── DIRECTORIES ────────────────────────────────────────────
PDF_OUTPUT_DIR=./out/pdfs        # Where generated PDFs are written (defaults to ".")
USER_PREFS_DIR=./data/user_prefs # JSON prefs live here (defaults to ".")
PHOTO_DIR=./data/photos          # Mandatory – image source/destination
